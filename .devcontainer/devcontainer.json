{
  "name": "Rixens HA Dev",
  "dockerFile": "Dockerfile",
  "postCreateCommand": "pip install -r requirements.txt -r requirements_test.txt && pip install pre-commit && pre-commit install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.black-formatter",
        "ms-python.flake8",
        "EditorConfig.EditorConfig"
      ]
    }
  },
  "forwardPorts": [8123],
  "remoteEnv": {
    "PYTHONUNBUFFERED": "1"
  }
}